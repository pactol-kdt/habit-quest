# Loaded automatically by `next dev` (NODE_ENV=development).
# Override with `.env.development.local` if needed (gitignored).

AUTH_SECRET=dev-only-change-me-habitquest-secret-key

# Local PostgreSQL (pgAdmin / Docker / Homebrew defaults)
DATABASE_URL=postgresql://postgres@127.0.0.1:5432/habitquest

# Web Push (VAPID). Regenerate with: npx web-push generate-vapid-keys --json
NEXT_PUBLIC_VAPID_PUBLIC_KEY=BE164pUOjKdIAMQHbWiwwfv54beIya0ntJaFPFx6gCmhecsNhXYz3yowM8XudzWg5O1B4J9Ekp7uMW8OERTIZH4
VAPID_PRIVATE_KEY=ccpD5YSMt-vGOeb2fLw5LQpTTfQpx33esfojxV0A5qQ
VAPID_SUBJECT=mailto:dev@habitquest.local

# Optional: protect /api/cron/reminders in production (Bearer token)
# CRON_SECRET=replace-me

# Optional: force this email to admin on signup (first account is always admin)
# ADMIN_EMAIL=you@example.com
